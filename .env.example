# Environment variables for local development
# Copy this file to `.env` and fill in values as needed.

# Optional: Set the branch name for local builds (used by the BranchBadge)
VITE_BRANCH_NAME=

# Optional: Override Supabase credentials locally
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
